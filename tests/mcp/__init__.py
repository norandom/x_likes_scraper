"""Tests for the ``x_likes_mcp`` package.

This subtree is import-isolated from the main ``tests/`` suite via its own
``conftest.py`` which adds an extra "no real LLM" guard on top of the
session-level network and cookies guards declared in ``tests/conftest.py``.
"""
